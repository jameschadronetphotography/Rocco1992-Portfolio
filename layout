<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ page.title }} | {{ site.title }}</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    body { margin:0; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; background:#0e0e0f; color:#eaeaea; }
    header, footer { padding:16px 24px; border-bottom:1px solid #2a2a2d; }
    a { color: inherit; text-decoration:none; }
    .container { max-width:1100px; margin: 24px auto; padding: 0 16px; }
    .grid { display:grid; gap:12px; grid-template-columns: repeat(auto-fill, minmax(220px,1fr)); }
    .card { background:#141416; border:1px solid #242428; border-radius:10px; overflow:hidden; }
    .card img { width:100%; height:220px; object-fit:cover; display:block; }
    .card .label { padding:10px 12px; font-weight:600; }
    .breadcrumbs { color:#9a9aa1; font-size:14px; margin-bottom:12px; }
  </style>
</head>
<body>
  <header>
    <strong>{{ site.title }}</strong>
  </header>
  <div class="container">
    {{ content }}
  </div>
  <footer class="container" style="border-top:1px solid #2a2a2d; padding-top:16px; margin-top:24px;">
    © {{ site.title }} {{ "now" | date: "%Y" }}
  </footer>
</body>
</html>
